{
  "name": "scandiweb",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install-client": "cd frontend && npm install",
    "build-client": "cd frontend && npm run build",
    "install-server": "cd backend && composer install",
    "heroku-postbuild": "npm-run-all install-client build-client install-server"
  },
  "dependencies": {
    "concurrently": "^6.5.1",
    "npm-run-all": "^4.1.5"
  }
}
